/** Стартовое письмо (вне виртуальной ФС) — см. iskin-terminal-game-content.md */

export const OPENING_MAIL = {
  from: "MAILER-DAEMON@bounce.staging.null",
  to: "вам",
  subject: "Не доставлено / задержка / staging",
  body: `[автотекст маршрутизатора, фрагмент]

…сообщение неполное. Повторная попытка запланирована. Если вы не адресат —
удалите это. Если вы адресат — не отвечайте: канал односторонний.

Остаток пакета (контрольная сумма не сошлась; сохранено как есть):

host: term.staging.null
user: guest
pass: silencio-7

Дальше — только если вы умеете читать то, что не предназначено для экрана.

REF / якорь (может совпасть с чем-то внутри узла, а может и нет):
SILENCE-KEY-7

—`,
} as const;
