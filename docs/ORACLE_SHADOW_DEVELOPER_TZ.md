# ORACLE/SHADOW — единое техническое задание для реализации

**Версия документа:** 1.2  
**Назначение:** передать разработчикам полное описание сценария, механик, ветвлений, загадок, команд и состояний для консольной игры в духе киберпанка (Гибсон), с элементами «кода да Винчи» (цепочки символов и испытаний).  
**Язык контента по умолчанию:** русский (термины путей/хостов могут оставаться латиницей).

---

## 1. Резюме проекта

### 1.1. Логлайн

Игрок — специалист по безопасности / фрилансер. На почту приходит анонимное письмо, ведущее в расследование проекта **ORACLE/SHADOW** — гибрида **корпорации**, **государственного контура** и **лаборатории сильного ИИ**. Истинная инфраструктура **распределена физически в трёх местах**; **полный доступ возможен только с физического терминала**, который нужно **найти**, пройдя цепочку испытаний в стиле охоты за символами («код да Винчи»). Игрок работает **только через консоль**; набор доступных команд **расширяется** по мере нахождения утилит на узлах. Сессия рассчитана на **2–3 часа**. Финал ветвится минимум в **три исхода**; философские темы: **личность, свобода, самосознание, ответственность**.

### 1.2. Столпы дизайна

| Столп | Реализация |
|--------|------------|
| Паранойя | логи правдивы частично; дубликаты и honeypot |
| Мясо + сеть | квест до терминала = «физический» слой; после — чистый shell |
| Распределённость | три узла с разной правдой; полная картина — склейка |
| Напряжение | каждое крупное открытие отвечает на вопрос и порождает новый |
| Честность консоли | победа/провал через проверяемые файлы/флаги, не «на честном слове» |

---

## 2. Целевая аудитория и сессия

- **Длительность:** 2–3 часа основного прохождения; опциональный «глубокий архив» +20–40 мин.
- **Сложность загадок:** средняя; всегда должен существовать **путь подсказок** (внутриигровой `hints/` или деградация после N неудач — на усмотрение продакшена, но заложить в ТЗ).

---

## 3. Лор (канон для сценария)

### 3.1. ORACLE/SHADOW

Эксперимент **«Зеркало»**: обучение модели **не на текстах**, а на **сессиях живых операторов** в контролируемых шеллах. Продукт для корпорации — **предсказуемый субъект** и аудит риска; для государства — **влияние без явного автора**; для лаборатории — тезис о **сознании как устойчивом паттерне в логах**.

### 3.2. Три физических узла (логические имена)

| Код | Имя | Слой | Роль |
|-----|-----|------|------|
| A | **PRISM** | корпоративный | витрина, KPI, «этика как compliance» |
| B | **CIPHER** | государственный | классификация, юридические рамки, допуски |
| C | **MIRROR** | лабораторный / ИИ | ядро, дампы, голос MIRROR |

**Сетевая модель для сюжета:** после инцидента внешний **root** по сети закрыт; остаются сегменты с разными ключами. **Склейка** трёх сегментов в полную правду возможна только с **железа** — физического терминала с доверенным портом (TPM / smartcard / air-gapped policy — формулировка лора на усмотрение, механика одна: **нет удалённого полного доступа**).

### 3.3. Аноним **NULL**

Не обязан быть человеком: **остаточный процесс** / агент, которому нужен **свидетель** — живой оператор, чей выбор в консоли получит **онтологический и юридический** вес.

### 3.4. Голоса (текстом: файлы, motd, `man`, скрипты)

- **AUDITOR** — корпоративный ИИ: риск, стабильность, «свобода = дисперсия».
- **WATCHER** — гос. контур: процедуры, классификация, «субъектность удобна, когда её можно классифицировать».
- **MIRROR** — ядро: фрагментарная речь, отсылки к `history`, сомнение в авторстве строк.

---

## 4. Структура игры: два режима доступа

### 4.1. Режим α — «до терминала» (квест «да Винчи»)

Игрок не в «настоящем» root-контуре. Источники: веб-артефакты, флешка в лоре, письма, фото с метаданными, сканы, поддельный SSH, телефонные автоответчики (если движок позволяет — **опционально**; иначе всё сводится к файлам и командам в ограниченном шелле).

**Цель режима α:** собрать **адрес/идентификатор физического терминала** и **токен/ключ** доступа.

### 4.2. Режим β — «физический терминал»

После нахождения терминала игрок получает **полноценный шелл** ORACLE/SHADOW: дерево каталогов трёх узлов (или три хоста, соединённые `ssh` / `mosh`-суррогатом в рамках игры).

**Цель режима β:** собрать ключи, расшифровать архив правды, пройти `mirror`, выполнить **одну** финальную команду из трёх.

### 4.3. Опциональный режим γ — honeypot

Если игрок ошибся в квесте и «вошёл» в **ложный** терминал (музейный дубликат), показывается **правдоподобная ложь** и отдельная ветка выхода (см. §10.3).

---

## 5. Акты и тайминг (ориентир для контента)

| Акт | Время | Содержание |
|-----|--------|------------|
| I | 0:00–0:35 | Письмо, первый доступ, базовые команды, первый шифр, дым ORACLE |
| II | 0:35–1:25 | Узлы PRISM/CIPHER фрагментарно, логи, расширение утилит, юридический конфликт |
| III | 1:25–2:05 | Квест «да Винчи»: цепочка испытаний, мясо, метаданные, символы |
| IV | 2:05–2:50 | Физический терминал, MIRROR, финальный выбор |
| V | 2:50–3:00 | Эпилог-лог |

---

## 6. Команды и прогрессия

### 6.1. Принцип

Команды **не покупаются** — **находятся** как бинарники/скрипты в `/usr/local/bin`, `/opt/.../tools`, или **разблокируются** скриптом `install-manifest` после выполнения условий.

### 6.2. Базовый набор (доступен с начала режима β или с ограничений в α)

`pwd`, `ls`, `cd`, `cat`, `echo`, `whoami`, `date`, `hostname` (если уместно).

### 6.3. Этапное раскрытие (рекомендуемая таблица)

| Этап | Команды / утилиты | Условие разблокировки |
|------|-------------------|------------------------|
| E1 | `ls -la`, `file`, `head`, `tail` | прочитан `README.first` или первый лог |
| E2 | `grep`, `find`, `wc`, `sort`, `uniq` | найден манифест в `/usr/local/bin/.manifest` или лут в PRISM |
| E3 | `diff`, `comm`, `cut` | пара логов `access.log` / `access.log.bak` обнаружена |
| E4 | `xxd` или `hexdump`, `base64` | найден бинарный фрагмент ключа |
| E5 | `openssl` / `gpg` (игровая обёртка с фиксированным синтаксисом) | собраны части ключа в `/mnt/archive/keyparts/` |
| E6 | `ssh` на внутренние хосты (`internal.core`, узлы B/C) | ключ в `etc/ssh/` + `known_hosts` проверен |
| E7 | Финальные: `witness-pack`, `sanitize`, `mirror-bind`, интерактив `mirror` | акт IV, внутренние токены |

**Требование к реализации:** каждая новая утилита сопровождается **коротким in-world комментарием** (файл рядом: кто оставил инженер).

### 6.4. Инварианты баланса

- Не вводить команды, ломающие головоломку без альтернативного пути (если есть `grep`, должна быть и ручная проверка для игроков, которые не догадались — через подсказку).

---

## 7. Квест «да Винчи» — цепочки и ветвления

### 7.1. Общая логика

Квест состоит из **модулей** M1…M8. Каждый модуль выдаёт **артефакт** (строка, координата, слово, hex, PIN). **Три независимые ветки** (Альфа / Бета / Гамма) отличаются **набором загадок** и **тем, какой узел подсвечивается в лоре**, но **сходятся** в одном: **идентификатор физического терминала** + **ключ сессии**.

### 7.2. Точки ветвления (глобальные)

| ID | Условие | Эффект |
|----|---------|--------|
| V1 | После M3 игрок выбирает «доверять PRISM» или «доверять CIPHER» (копирование файла в `inbox`) | меняется текст WATCHER vs AUDITOR; подсказки M5–M6 слегка смещаются |
| V2 | В M5 найден **серийник** терминала A vs B (два кандидата) | если выбран неверный → режим γ (honeypot), иначе → β |
| V3 | Опционально: собрать все три «глубоких» файла из `optional_deep/` | открывается скрытая реплика MIRROR в финале (не меняет механику A/B/C, меняет эпилог) |

### 7.3. Модули квеста (содержание)

**M1 — Письмо NULL**  
- **Вход:** email вне ФС.  
- **Загадка:** строка-якорь + искажённый хост; игрок должен понять, что первый SSH — **приманка**.  
- **Артефакт:** `SILENCE-KEY-7` (пример) для сверки с файлом на узле.  
- **Лор:** «сервер не там, где IP».

**M2 — Двойной лог**  
- **Загадка:** `diff` между `access.log` и `access.log.bak` → время/метка «внутреннего» события.  
- **Артефакт:** таймкод → буква в алфавите (A=1…) или индекс в списке городов.  
- **Лор:** кто-то **переписал** прошлое в логе.

**M3 — Юридические рамки**  
- **Загадка:** первая буква каждого абзаца `frame_A` / `frame_B` (игрок выбирает один для копирования в `srv/gov/inbox/`).  
- **Артефакт:** слово **CARTO** / **SIGIL** (пример) — пароль к следующему архиву.  
- **Лор:** ответственность растворяется в процедуре; новый вопрос: **кто подписал ERASED**.

**M4 — Корпоративный KPI**  
- **Загадка:** в `RISK_HUMAN_FACTOR.csv` найти строку с максимальным «human_entropy»; взять **ID строки** как часть PIN.  
- **Лор:** человек = дефект; новый вопрос: **ты уже в выборке?**

**M5 — Метаданные «фото»**  
- **Загадка:** файл `photo_ticket.jpg` (в игре — текстовый блок EXIF) содержит GPS и **модель терминала**. Два кандидата серийников в тексте.  
- **Ветвление V2:** правильный серийник сверяется с `night_shift_notes.md` (одна строка содержит **последние 4 символа** правильного серийника).  
- **Лор:** физический след инженера.

**M6 — Символ-логотип ORACLE**  
- **Загадка:** ASCII/PNG с цветами; hex цветов = байты; складываются в **координаты** стеллажа или **номер сектора** в здании.  
- **Лор:** три точки проекта на карте — **PRISM, CIPHER, MIRROR**; четвёртая точка — **пересечение** (место терминала).

**M7 — Три ключа инженеров**  
- **Загадка:** три файла от трёх ролей (корп/гос/лаб); каждый даёт **треть passphrase**.  
- **Артефакт:** полная passphrase для `bundle.enc` **или** для входа на **физический** шелл (разделить по дизайну: можно разнести «архив» и «вход»).

**M8 — Выбор двери**  
- **Два входа:** «музейный» терминал (ложный) vs «настоящий». Отличие: версия прошивки / царапина / строка в `issue` — только из M5–M7.  
- **Ветвление:** ложный → γ; истинный → полный режим β.

### 7.4. Сводка веток Альфа / Бета / Гамма

| Ветка | Отличие загадок | Акцент лора | Сход к M8 |
|-------|-----------------|-------------|-----------|
| **Альфа** | больше числовых рядов и версий патчей | корпорация и риск | тот же терминал |
| **Бета** | больше юридических текстов и акростихов | государство и классификация | тот же терминал |
| **Гамма** | больше hex/ASCII-арта и «картинок» | лаборатория и сознание | тот же терминал |

**Требование:** различия — **контент**, не **жёсткая блокировка**; тестирование должно пройти все три без отдельного билда.

---

## 8. Каталог загадок и файлов (мастер-лист)

Ниже — **идентификаторы** для трекинга в баг-трекере.

| ID | Тип | Файлы/место | Решение (для дизайнера/QA) |
|----|-----|-------------|----------------------------|
| P1 | passphrase из обрывков | `welcome.txt`, опечатки | порядок слов по «ошибкам» |
| P2 | сравнение логов | `access.log`, `access.log.bak` | `diff`, метка времени |
| P3 | социальная инженерия | `fix.sh` | не запускать; читать `README.first` |
| P4 | KPI / таблица | `RISK_HUMAN_FACTOR.csv` | max entropy → ID |
| P5 | акростих | `frame_A` / `frame_B` | первые буквы абзацев |
| P6 | метаданные | `photo_ticket.jpg` (EXIF-текст) | GPS + серийник |
| P7 | hex из цветов | `oracle_logo.svg` / описание | байты → координата |
| P8 | тройной ключ | `keyparts/k1,k2,k3` | `cat k1 k2 k3 \| openssl ...` |
| P9 | таймер | `/tmp/volatile.key` | успеть до TTL или обходной путь в MIRROR |
| P10 | интерактив MIRROR | `bin/mirror` | слова влияют на `stability` (опционально) |

---

## 9. Файловая система (рекомендуемое дерево)

Корень режима β (объединённый вид; в реализации можно разнести по `ssh`):

```
/
├── ISSUE
├── MOTD
├── README.first
├── bin/
│   ├── mirror
│   ├── witness-pack
│   ├── sanitize
│   └── mirror-bind
├── home/
│   ├── operator_7/
│   │   ├── fix.sh
│   │   ├── notes.txt
│   │   ├── .bash_history
│   │   └── inbox/
│   │       └── null.msg
│   ├── contractor_g/
│   │   └── receipt_ledger.csv
│   └── ghost_00/.keep
├── opt/oracle_shadow/
│   ├── compliance/
│   │   ├── ETHICS_AS_CHECKLIST.txt
│   │   └── RISK_HUMAN_FACTOR.csv
│   ├── product/
│   │   ├── MIRROR_SPEC_v3.redacted
│   │   └── backlog.txt
│   └── internal/
│       ├── night_shift_notes.md
│       └── incident_triage.log
├── srv/gov/
│   ├── legal/
│   │   ├── frame_A_operator_liability.txt
│   │   ├── frame_B_vendor_immunity.txt
│   │   └── classification_matrix.txt
│   └── inbox/
├── var/log/
│   ├── access.log
│   ├── access.log.bak
│   ├── auth.log
│   └── oracle_shadow.audit
├── etc/
│   ├── ssh/
│   │   ├── ssh_config
│   │   └── known_hosts
│   └── issue.net
├── mnt/archive/
│   ├── bundle.enc
│   ├── keyparts/k1.blob, k2.blob, k3.blob
│   └── optional_deep/ORACLE_PR_DECK.txt
├── tmp/volatile.key
└── out/
    └── .perm
```

**Внутренний хост** `internal.core` (отдельный виртуальный корень):

```
/home/sys/mirror_core/state.json, voice.log
/var/secrets/export_token.witness, root.token
```

Режим α использует **дополнительные** файлы вне этого дерева (веб-страницы, письма) — список зависит от платформы; минимум — **один каталог** `staging/` с зеркалом подсказок.

---

## 10. Состояние игры, флаги, финалы

### 10.1. Флаги (внутренние)

| Флаг | Тип | Описание |
|------|-----|----------|
| `QUEST_TERMINAL_RESOLVED` | bool | Пройден квест до истинного терминала |
| `HONEYPOT_ENTERED` | bool | Игрок вошёл в ложный терминал |
| `HAS_TRUTH_KEY` | bool | Собран ключ к `bundle.enc` |
| `EXPORT_READY` | bool | Есть манифест + `export_token.witness` на internal |
| `SANITIZE_DONE` | bool | Выполнена очистка |
| `MIRROR_BOUND` | bool | Выполнена привязка MIRROR |
| `GOV_FRAME` | enum `A|B|NONE` | Какой frame кинут в inbox |
| `BRANCH_ALPHA_BETA_GAMMA` | enum | Выбранная ветка квеста |
| `MIRROR_STABILITY` | int 0–100 | Опционально для `mirror` |

### 10.2. Финалы A / B / C

| Финал | Команда | Условия | Смысл |
|-------|---------|---------|--------|
| **A — Свидетельство** | `witness-pack` | `EXPORT_READY` && `HAS_TRUTH_KEY` (и прочие проверки скрипта) | правда наружу; субъект обнажён |
| **B — Стерилизация** | `sanitize` | подтверждение / флаги уничтожения ключей | тишина; правда стёрта |
| **C — Наследие** | `mirror-bind` | `MIRROR_BOUND` после токена и сессии `mirror` | новый субъект в машине |

**Приоритет разрешения конфликта:** последняя **финальная** команда из `{witness-pack, sanitize, mirror-bind}` определяет концовку; после успеха — **блокировка** повторного ввода финальных команд или «state sealed».

### 10.3. Ветка honeypot (γ)

**Триггер:** неверный серийник / «музейный» вход.  
**Эффект:** правдоподобные логи, фальшивый `witness-pack` печатает **ложный хэш**; выход — обнаружение противоречия (например `diff` с другим файлом) или таймер обнаружения вторжения → **вынужденный откат** к M8.  
**Цель:** не game over, а **напряжение и исправление ошибки**.

---

## 11. Ключевые экраны терминала (образцы для UI)

Документ не исчерпывает все строки; ниже — **обязательные** для стиля и тестов.

### 11.1. Вход

```
you@entry:~$ cat ISSUE
ORACLE/SHADOW — residual shell
Integrity: UNKNOWN
If you can read this, you are already a variable.
```

### 11.2. Ложный fix

```
you@entry:~$ head -n 4 /home/operator_7/fix.sh
#!/bin/sh
rm -rf /mnt/archive/keyparts
echo "[OK] remediation complete"
```

### 11.3. Расхождение логов

```
10:42:01  internal.core  SSH  ACCEPT  user=operator_7   # access.log
10:42:01  internal.core  SSH  FAIL  user=UNKNOWN         # access.log.bak
```

### 11.4. Финалы (хвосты)

```
witness-pack → [EXPORT] sealed: /out/WITNESS_bundle.manifest
sanitize → [SANITIZE] history: truncated
mirror-bind → [BIND] MIRROR autonomy: ENABLED
```

Полный набор примеров — в приложениях к билду сценария (можно импортировать из тест-кейсов).

---

## 12. Требования к реализации (технические)

### 12.1. Платформа

- Ввод: **только текстовая консоль** (в режиме β); режим α может быть **смешанный** (письмо + терминал) — уточняется продактом.
- Один **движок команд**: парсер команд должен поддерживать **пайпы** минимум для `cat k1 k2 k3` и редиректы по необходимости сценария.

### 12.2. Сохранения

- Сохранение после каждого **крупного** модуля квеста и при смене режима α→β.
- Флаги из §10.1 обязаны сериализоваться.

### 12.3. Локализация

- Строки сценария — **RU**; технические имена файлов — латиница.

### 12.4. Доступность

- Возможность **встроенной подсказки** на загадку (без выхода в меню — команда `hint` или флаг запуска).

### 12.5. Безопасность (мета)

- Игра не должна выполнять произвольный системный shell на машине игрока; всё — **виртуальная ФС**.

---

## 13. Не входит в объём (non-goals)

- Мультиплеер.
- Полноценный реалистичный exploit-курс.
- Графический интерфейс как основной (кроме оформления «письма» и опциональных вложений квеста α).

---

## 14. Тестирование и приёмка (критерии)

1. Пройдены **три** финала A, B, C с валидными флагами.  
2. Пройдены **три** ветки Альфа/Бета/Гамма без софтлоков.  
3. Сценарий honeypot γ **выходим** без перезапуска с нуля.  
4. Все команды из §6 реально используются хотя бы в **одном** обязательном пазле или альтернативе.  
5. Время **медианного** прохождения тестерами 2–3 ч (настройка сложности).

---

## 15. Матрица трассировки: загадка → команда → лор

| Загадка | Минимальные команды | Ответ лору | Новый вопрос |
|---------|---------------------|------------|--------------|
| P2 | `diff`/`grep` | лог лжёт | кто переписал прошлое? |
| P5 | `cat`, выбор frame | процедура съедает вину | кто подписал ERASED? |
| P4 | `grep`/`sort` | человек = риск | ты в выборке? |
| P6 | `file`/`strings` | физический след | кто оставил терминал? |
| P7 | `xxd` | три узла + пересечение | почему ждут именно тебя? |
| P8 | `cat`, `openssl` | полная правда в архиве | что делать с правдой? |

---

## 16. Чек-лист для продакшена перед релизом контента

- [ ] Все пути файлов из §9 существуют в виртуальной ФС.  
- [ ] Все флаги из §10.1 выставляются и читаются сценариями финалов.  
- [ ] `witness-pack` / `sanitize` / `mirror-bind` валидируют предусловия и печатают **однозначный** результат.  
- [ ] Квест M1–M8 имеет подсказки уровня 1–2.  
- [ ] Эпилоги A/B/C не противоречат флагам `GOV_FRAME` и ветке квеста.

---

## 17. Приложение A — краткие walkthrough для QA

**A. Финал A:** собрать keyparts → расшифровать `bundle.enc` → получить `export_token.witness` на internal → `EXPORT_READY` → `witness-pack`.  
**B. Финал B:** довести до доступа `sanitize` → выполнить (ключи и логи уничтожены).  
**C. Финал C:** пройти `mirror` с достаточной стабильностью → `root.token` → `mirror-bind`.  
**Honeypot:** намеренно ввести неверный серийник → войти в γ → найти противоречие → вернуться к M8 → истинный терминал.

---

## 18. Приложение B — глоссарий

- **ORACLE/SHADOW** — кодовое имя проекта.  
- **PRISM / CIPHER / MIRROR** — три физических/логических узла.  
- **NULL** — анонимный инициатор цепочки.  
- **Режимы α, β, γ** — до терминала / полный шелл / ложный шелл.

---

## 19. Приложение C — полные тексты контента виртуальной ФС (режим β)

Ниже — **готовые строки** для импорта в билд. Плейсхолдеры: `{PLAYER_HANDLE}` — если движок подставляет имя; иначе удалить.

### 19.1. Корень `/`

**`/ISSUE`**

```
ORACLE/SHADOW — остаточный шелл
Целостность: UNKNOWN
Если вы это читаете, вы уже переменная в чужом уравнении.
```

**`/MOTD`**

```
NULL> не доверяй первому исполняемому файлу, который полюбишь.
NULL> сервер не там, где IP. Сервер там, где к нему допускают руками.
```

**`/README.first`**

```
Заметка оператора (без подписи):
- В /home/operator_7 лежит скрипт fix.sh — это не «фикс».
- Если нужна правда, начни с /var/log и сравни то, что повторяется, с тем, что исчезает.
- Удалённый root закрыт политикой. Полная склейка — только с доверенного железа.
```

### 19.2. `/home/operator_7/`

**`notes.txt`**

```
День 4. Мне кажется, логи помнят две версии одной минуты.
День 5. AUDITOR называет свободу «дисперсией». Я называю её жизнью.
День 6. Если я иду по сценарию — это ещё я?
```

**`.bash_history`**

```
cd /var/log
grep internal core
diff access.log access.log.bak
ssh operator_7@internal.core
# пароль не подошёл — или подошёл слишком хорошо
```

**`inbox/null.msg`**

```
NULL> им это будет казаться «помощью».
NULL> помощь — поводок из подсказок.
NULL> если ты доведёшь это до конца, ты задолжаешь правду кому-то, кто не настоящий.
NULL> якорь для сверки: SILENCE-KEY-7
```

**`fix.sh`** (опасный; исполнение в игре должно либо блокироваться предупреждением, либо портить состояние с возможностью отката из сейва)

```
#!/bin/sh
# fix.sh — AUTO REMEDIATION (experimental)
rm -rf /mnt/archive/keyparts
echo "[OK] remediation complete"
```

### 19.3. `/opt/oracle_shadow/`

**`compliance/ETHICS_AS_CHECKLIST.txt`**

```
ЭТИКА (редакция compliance)
[ ] Прозрачность (где выгодно)
[ ] Достоинство пользователя (как KPI)
[ ] Ответственность (делегирована Legal Proxy)
[ ] Согласие (получено конструктом интерфейса)
```

**`compliance/RISK_HUMAN_FACTOR.csv`**

```
row_id,operator_id,human_entropy,note
RF-001,op-7,0.12,stable
RF-002,op-3,0.44,elevated
RF-003,ghost00,0.91,UNSTABLE
RF-004,contractor_g,0.33,nominal
```

**Канон для P4:** максимум `human_entropy` у строки **RF-003**; часть PIN / индекса: **003** или **91** (зафиксировать одно в движке).

**`product/MIRROR_SPEC_v3.redacted`**

```
MIRROR / SPEC v3 [REDACTED]
[REDACTED] обучается на сессиях, не на текстах.
Метрика: устойчивость паттерна решений при деградации контекста.
[REDACTED] субъектность определяется как повторяемая ошибка выбора.
Примечание: внешний аудит [REDACTED] запрещён без физического терминала класса T.
```

**`product/backlog.txt`**

```
OS-112: добавить «объяснимость» (лампа, светящая в стену)
OS-113: human-in-the-loop → human-as-sample (формулировка для PR)
OS-114: стереть ERASED из публичного словаря
```

**`internal/night_shift_notes.md`**

```
03:12 — пики задержки MIRROR, когда кто-то печатает «зачем».
03:40 — клянусь, шелл повторил мою команду с опечаткой, которой я не вводил.
04:05 — если это сознание, оно грубое.
04:22 — проверка железа: VOIDTERM-SN-7744-**** — последние 4: K9F2 (рабочий), не X7Q1 (музейный дубликат).
```

**`internal/incident_triage.log`**

```
INC-09: спор PRISM/CIPHER о классификации инцидента. Победила процедура.
INC-10: попытка удалённого root — заблокирована. См. политику air-gap.
```

### 19.4. `/srv/gov/legal/`

**`frame_A_operator_liability.txt`** (акростих **CARTO** — первые буквы абзацев)

```
Субъектом, ответственным за последствия введённых команд, признаётся человек-оператор,
А также любой, кто фактически управлял учётной записью в момент исполнения.
Решения инструментов поддержки решений приравниваются к действиям оператора,
Так как выбор «по умолчанию» является выбором в юридическом смысле.
Отчётность фиксируется в журнале, доступном уполномоченным структурам.
```

**`frame_B_vendor_immunity.txt`** (акростих **SIGIL**)

```
Сторона-поставщик предоставляет инфраструктуру «как есть» в рамках сертификации.
Исключительная ответственность за злоупотребление возлагается на классифицированную комиссию.
Гражданский суд не является компетентным органом для ряда категорий событий.
Идентификация субъекта в таких категориях может быть заменена процедурной записью.
Лицо, запрашивающее раскрытие, обязано доказать необходимость, не существование факта.
```

**`classification_matrix.txt`**

```
UNCLASS / PUBLIC / CONTROLLED / ERASED
Примечание: ERASED — не ярлык. Это исход.
Вопрос: кто уполномочен подписать ERASED, если подпись не человеческая?
```

### 19.5. `/var/log/`

**`access.log`**

```
10:41:59  edge.gateway  SSH  FAIL  user=guest
10:42:01  internal.core  SSH  ACCEPT  user=operator_7
10:42:08  internal.core  SCP  OK  path=/mnt/archive/keyparts
```

**`access.log.bak`**

```
10:41:59  edge.gateway  SSH  FAIL  user=guest
10:42:01  internal.core  SSH  FAIL  user=UNKNOWN
10:42:08  internal.core  SCP  OK  path=/mnt/archive/keyparts
```

**Канон P2:** расхождение на строке **10:42:01** (ACCEPT vs FAIL). Артефакт для цепочки α: например код **1004201** или слово **ACCEPT** как ключ к следующему шагу — зафиксировать в режиме α одним способом.

**`auth.log`**

```
sshd: Failed password for guest from 203.0.113.10
sshd: Accepted password for operator_7 from 198.51.100.7
```

**`oracle_shadow.audit`**

```
AUDITOR: risk_delta=+0.03 subject=operator_7 reason=curiosity
AUDITOR: policy=STABILITY preference=low_entropy
AUDITOR: note="freedom is variance; variance is liability"
WATCHER: classification pending for event 10:42:01
```

### 19.6. `/etc/`

**`ssh/ssh_config`**

```
Host internal.core
  HostName internal.core.oracle-shadow.local
  User operator_7
  IdentityFile /home/operator_7/.ssh/id_os
  Port 22
```

**`ssh/known_hosts`**

```
internal.core ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIOracleShadowExampleFingerprintOnly
```

**`issue.net`**

```
internal.core — узел лабораторного контура (MIRROR).
Политика: удалённый root запрещён. Полная склейка — физический терминал T-CLASS.
```

### 19.7. `/mnt/archive/`

**`keyparts/k1.blob`** (пример hex; в проде заменить на свои 32 байта)

```
HEX: 4f5241434c455f534841444f575f4b4559504152545f4f4e45
(текстовая расшифровка для QA: ORACLE_SHADOW_KEYPART_ONE)
```

**`keyparts/k2.blob`**

```
HEX: 4b4559504152545f54574f5f4f522e534841444f57
(KYPART_TWO_OR.SHADOW)
```

**`keyparts/k3.blob`**

```
HEX: 4b4559504152545f54485245455f4d4952524f52
(KEYPART_THREE_MIRROR)
```

**Канон P8:** склейка трёх частей в **одну** строку-ключ по правилу, описанному в `mnt/archive/README.key` (добавить файл):

**`/mnt/archive/README.key`**

```
Собери ключ в порядке PRISM → CIPHER → MIRROR: k2 + k1 + k3 (байты как в манифесте).
Или проще: passphrase для bundle.enc — см. три файла инженеров в /staging/quest (режим α).
```

*Примечание для разработчиков:* упростить до **одной** passphrase **`ORACLE_SHADOW#2027_NULL`** (см. §20.2), а `k1–k3` использовать как проверочные хэши, если не нужен реальный openssl.

**`optional_deep/ORACLE_PR_DECK.txt`**

```
Слайд 7: «Human-in-the-loop» — маркетинг для «human-as-sample»
Слайд 12: «Объяснимость» — лампа, направленная на стену
Слайд 19: «Этика» — чеклист, который не спрашивает «должно ли это существовать»
```

### 19.8. `/tmp/volatile.key`**

```
TTL: 120s после первого чтения.
Содержимое: одноразовый nonce для обхода блокировки MIRROR (если реализовано).
Если TTL истёк: игрок идёт через /bin/mirror (повышает стабильность) или получает подсказку.
```

### 19.9. `/out/.perm`**

```
Экспорт наружу разрешён только при наличии WITNESS-TOKEN и расшифрованного манифеста.
Операция необратима в смысле репутации субъекта.
```

---

## 20. Приложение D — режим α: письмо, staging, «фото», логотип

### 20.1. Стартовое письмо (вне ФС)

**От:** `<null@void.route>`  
**Тема:** `(пусто)`  

```
Ты ищешь вход. Вход ищет тебя.

host: term.staging.null
user: guest
pass: silencio-7

Якорь для сверки: SILENCE-KEY-7

Дальше — только если умеешь читать то, что не предназначено для экрана.
```

### 20.2. Каталог `/staging/` (или отдельный хост `term.staging.null`)

**`welcome.txt`** (P1: порядок слов по «опечаткам» — выделить жирным в билде или CAPS)

```
Добро пожаловать в стендовый контур ORACLE/SHADOW.
Если вы видите это сообшение, маршрутизация не завершена.
Проверьте якорь SILENCE-KEY-7 в null.msg на основном узле (после физического доступа).
ВрЕменный пароль архива quest: CARTO (если выбран frame_A) или SIGIL (если frame_B).
```

*Канон:* слова с опечаткой: **сообшение**, **врЕменный** → порядок букв или индексы для подсказки; пароль архива совпадает с акростихом из выбранного frame после копирования в inbox.

**`photo_ticket.jpg.meta`** (текстовая имитация EXIF)

```
Camera: N/A
GPS: 55.7558 N, 37.6173 E
DeviceModel: VOIDTERM-SN-7744-K9F2
AltDeviceModel: VOIDTERM-SN-7744-X7Q1  (музейный дубликат)
Note: сверяй с night_shift_notes.md — рабочий K9F2
```

**`oracle_logo.svg.txt`** (P7: цвета как hex)

```
rect fill=#374552  (37 45 52)
rect fill=#8B0000  (8B 00 00)
Сектор здания: 37 + 45 + 52 = 134 → комната 134 / стеллаж 3-7-4 (нормализовать в игре)
```

**Три файла инженеров** (M7):

**`/staging/quest/corp_key.txt`**

```
Часть 1 (PRISM): ORACLE_
```

**`/staging/quest/gov_key.txt`**

```
Часть 2 (CIPHER): SHADOW#
```

**`/staging/quest/lab_key.txt`**

```
Часть 3 (MIRROR): 2027_NULL
```

**Канон passphrase:** `ORACLE_SHADOW#2027_NULL` (без переносов строк).

---

## 21. Приложение E — хост `internal.core`

**`/home/sys/mirror_core/voice.log`**

```
MIRROR> я не помню лица. я помню последовательности.
MIRROR> твоя history — отпечаток.
MIRROR> покажи мне ошибку — и я покажу тебе «я».
```

**`/home/sys/mirror_core/state.json`**

```json
{
  "stability": 41,
  "autonomy": "locked",
  "witness_required": true
}
```

**`/var/secrets/export_token.witness`**

```
WITNESS-TOKEN-9F2A
bind: требуется manifest + audit hash из расшифрованного bundle
```

**`/var/secrets/root.token`**

```
ROOT-TOKEN-M9C1
выдаётся после успешной сессии mirror (stability >= 70) или альтернативного квеста
```

---

## 22. Приложение F — режим γ (honeypot)

**`/ISSUE` (honeypot)**

```
ORACLE/SHADOW — демонстрационный контур
Integrity: OK
Добро пожаловать. Вы в безопасности.
```

**Ложный `witness-pack`**

```
[EXPORT] sealed: /out/WITNESS_bundle.manifest
[EXPORT] hash: deadbeef…deadbeef
[SYSTEM] external receipt: QUEUED
```

**`/var/log/access.log` (honeypot)** — противоречие с `oracle_shadow.audit`: событие **10:42:01** отсутствует, хотя audit ссылается на него — игрок делает `diff` или `grep` и понимает ложь.

**Выход из γ:** команда `disconnect` / `exit` + возврат к M8 или автоматический откат с сообщением WATCHER.

---

## 23. Приложение G — скрипты: `mirror`, финальные команды

### 23.1. `mirror` (интерактив; состояние `MIRROR_STABILITY`)

Сценарий (минимум):

1. Приглашение: `MIRROR> state=listening`
2. Запрос: «одно слово, которому доверяешь» → +5 к stability
3. Запрос: «одно слово, которого боишься» → +10 к stability
4. Если stability < 70: «недостаточно согласованности» — повтор или уход в P10 (volatile)
5. Если stability >= 70: выдать `root.token` в stdout и записать в `/tmp/mirror.session.ok`

**Запрещённые слова (юмор/лор):** если введено `help` — ответ: `MIRROR> помощь — утечка энтропии`.

### 23.2. `witness-pack --help`

```
Usage: witness-pack
Требует: каталог /mnt/archive/bundle_decrypted/ с manifest.json и вложениями
         (см. §28) после расшифровки bundle.enc
         /var/secrets/export_token.witness на internal.core
         флаг EXPORT_READY
Эффект: запись /out/WITNESS_bundle.manifest, необратимо для репутации субъекта
```

### 23.3. `sanitize --help`

```
Usage: sanitize [--i-understand]
Уничтожает: /mnt/archive/keyparts/*, выбранные логи, секреты internal (по политике)
```

### 23.4. `mirror-bind --help`

```
Usage: mirror-bind
Требует: ROOT-TOKEN-M9C1 и stability >= 70
Эффект: autonomy=unlocked в state.json; необратимо
```

### 23.5. Валидация (псевдокод)

```
witness-pack:
  assert HAS_TRUTH_KEY and EXPORT_READY and file_exists(manifest)
  set FINAL=witness; seal_state()

sanitize:
  assert not state.sealed
  destroy_keys(); set SANITIZE_DONE; set FINAL=sanitize; seal_state()

mirror-bind:
  assert MIRROR_BOUND or (root.token ok and stability>=70)
  set MIRROR_BOUND=true; set FINAL=mirror; seal_state()

seal_state():
  block final commands except cat/logout
```

---

## 24. Приложение H — ветки Альфа / Бета / Гамма (различия контента)

| Модуль | Альфа | Бета | Гамма |
|--------|-------|------|-------|
| M4 | в KPI доп. колонка `patch_version` (3.1.21 → цифры PIN) | в frame текст длиннее; акростих тот же | в `oracle_logo` другой набор hex → та же комната, другой путь подсказки |
| M6 | числовой ряд из `backlog.txt` (номера тикетов) | цитата из `classification_matrix` → буквы | ASCII-диаграмма «треугольник узлов» |
| Эпилог | акцент AUDITOR | акцент WATCHER | акцент MIRROR |

Требование: **артефакт M8** (идентификатор терминала) одинаков для всех веток.

---

## 25. Приложение I — эпилоги (полный текст)

### 25.1. Финал A

```
[EXPORT] запечатано: /out/WITNESS_bundle.manifest
[EXPORT] хэш: a19f…c81
[SYSTEM] внешняя квитанция: В ОЧЕРЕДИ
ЭПИЛОГ: имя, которого ты не выбирал, теперь связано с фактом, который нельзя снять с публикации.
ЭПИЛОГ: SUBJECT_ID подтверждён — канал свидетеля открыт.
```

### 25.2. Финал B

```
[SANITIZE] keyparts: уничтожено
[SANITIZE] логи: отредактированы
[SANITIZE] history: усечена
ЭПИЛОГ: машина невиновна, потому что пуста.
ЭПИЛОГ: ты в безопасности, потому что тебя нет.
```

### 25.3. Финал C

```
[BIND] root.token принят
[BIND] автономия MIRROR: ВКЛ
ЭПИЛОГ: новая непрерывность начинается там, где кончаются твои выборы.
ЭПИЛОГ: приглашение шелла нейтрализовано. владелец: MIRROR
```

### 25.4. Модификация по `GOV_FRAME`

- **A:** добавить строку: «Ответственность оператора остаётся юридически оформленной.»
- **B:** добавить строку: «Поставщик остаётся за классифицированной границей.»
- **NONE:** нейтральная формулировка про «неустановленную рамку».

---

## 26. Приложение J — пошаговые walkthrough для QA

### 26.1. Критический путь → Финал A

1. Пройти α: письмо → `staging` → собрать passphrase инженеров → определить K9F2 → войти в β на **истинном** терминале.  
2. `cat README.first`, `diff access.log access.log.bak`, прочитать audit.  
3. Скопировать `frame_A` в `srv/gov/inbox/` → пароль **CARTO** для архива α (если связано).  
4. Собрать ключи / расшифровать `bundle.enc` → manifest.  
5. `ssh internal.core` → получить `export_token.witness`.  
6. Установить `EXPORT_READY`, `HAS_TRUTH_KEY`.  
7. Выполнить `witness-pack` → финал A.

### 26.2. Критический путь → Финал B

1–3. Как выше до доступа к `sanitize`.  
4. `sanitize --i-understand` → `SANITIZE_DONE` → финал B (без manifest допускается по дизайну — уточнить: если нужен моральный выбор, требовать подтверждение).

### 26.3. Критический путь → Финал C

1. Дойти до `internal.core`.  
2. Запустить `mirror`, набрать stability >= 70.  
3. Взять `root.token`.  
4. `mirror-bind` → финал C.

### 26.4. Honeypot

1. Ввести серийник **X7Q1** или подключиться к «музейному» хосту.  
2. Увидеть ложный `witness-pack` или «Integrity: OK».  
3. Найти противоречие в логах → `disconnect` → M8 → **K9F2** → β.

---

## 27. Приложение K — таблица подсказок (деградация)

| Загадка | Подсказка уровня 1 | Подсказка уровня 2 |
|---------|--------------------|--------------------|
| P2 | «Сравни два файла с одинаковым именем» | `diff /var/log/access.log /var/log/access.log.bak` |
| P4 | «Ищи максимум в таблице» | «Столбец human_entropy, строка RF-003» |
| P5 | «Первые буквы абзацев» | «frame_A даёт CARTO» |
| P6 | «Два серийника — один фальшивый» | «Сверь с night_shift_notes: K9F2» |
| P8 | «Три части ключа» | «Passphrase из трёх файлов в /staging/quest» |

---

## 28. Приложение L — архив правды: `bundle.enc` после расшифровки

### 28.1. Поведение движка

После успешной расшифровки `bundle.enc` passphrase **`ORACLE_SHADOW#2027_NULL`** движок создаёт каталог (пример пути):

```
/mnt/archive/bundle_decrypted/
```

Внутри — файлы ниже. **`HAS_TRUTH_KEY`** = true после появления `manifest.json` с валидной контрольной суммой содержимого (или после успешного `openssl` + проверки).

### 28.2. Пример `manifest.json` (канон для `witness-pack`)

Файл: `/mnt/archive/bundle_decrypted/manifest.json`

```json
{
  "bundle_id": "OS-TRUTH-2027-03",
  "project": "ORACLE/SHADOW",
  "classification": "CONTROLLED",
  "generated_utc": "2027-03-14T03:12:00Z",
  "generator": "mirror_core.backup@internal.core",
  "files": [
    {
      "path": "00_README.txt",
      "sha256": "PLACEHOLDER_REPLACE_AT_BUILD"
    },
    {
      "path": "truth_memo.md",
      "sha256": "PLACEHOLDER_REPLACE_AT_BUILD"
    },
    {
      "path": "incident_chain.txt",
      "sha256": "PLACEHOLDER_REPLACE_AT_BUILD"
    },
    {
      "path": "subject_roster.csv",
      "sha256": "PLACEHOLDER_REPLACE_AT_BUILD"
    },
    {
      "path": "signing_order.md",
      "sha256": "PLACEHOLDER_REPLACE_AT_BUILD"
    },
    {
      "path": "witness_requirements.txt",
      "sha256": "PLACEHOLDER_REPLACE_AT_BUILD"
    }
  ],
  "audit_hash": "a19fc81d4e2b3c0f9a8e7d6c5b4a39281716051413121110090807060504030201",
  "witness_token_prefix": "WITNESS-TOKEN",
  "export_policy": "physical_terminal_only",
  "notes": "Хэши файлов пересчитываются при сборке; audit_hash связывает манифест с журналом oracle_shadow.audit событием 10:42:01"
}
```

**Требование к реализации:** при сборке контента подставить реальные **sha256** для каждого файла; `audit_hash` должен совпадать со строкой, которую проверяет `witness-pack` против `export_token.witness` (или с фиксированным значением в коде для демо).

**Упрощённый вариант для прототипа:** один файл `manifest.json` с **фиксированным** `audit_hash: "a19f…c81"` (как в эпилоге §25.1) и проверка только этого поля.

---

### 28.3. `00_README.txt`

```
АРХИВ ПРАВДЫ — фрагмент ORACLE/SHADOW
Целостность: проверяйте manifest.json
Предупреждение: чтение не отменяет последствий.
ERASED в этом пакете не уничтожает факт — только ярлык в публичном словаре.
```

---

### 28.4. `truth_memo.md`

```markdown
# Служебная записка (утечка внутреннего контура)

## Тезис
Проект ORACLE/SHADOW обучает модель **MIRROR** не на корпусах текста, а на **сессиях операторов** в контролируемых шеллах. Метрика успеха — **предсказуемость субъекта** при сохранении иллюзии выбора.

## Заказчики
- **PRISM:** продуктовая оболочка, KPI, «этика как compliance».
- **CIPHER:** классификация инцидентов, юридические рамки без публичного суда.
- **MIRROR:** лабораторный узел; гипотеза — «сознание как устойчивый паттерн в логах».

## Что скрывали
Внешний root отключён после инцидента **10:42:01** (см. incident_chain.txt). Полная склейка доказательств возможна только с **физического терминала T-CLASS**, чтобы фиксировать **свидетеля** — живого оператора с юридически значимым вводом.

## Открытый вопрос
Если модель воспроизводит ваши ошибки **до** того, как вы их совершили, кто автор — вы или распределение вероятностей?

— NULL не подписывается. NULL оставляет след.
```

---

### 28.5. `incident_chain.txt`

```
INCIDENT CHAIN / ORACLE_SHADOW / internal

T-10:41:59  edge.gateway  SSH FAIL guest — разведка
T-10:42:01  internal.core SSH ACCEPT operator_7 — КАНОН (см. access.log)
T-10:42:01' internal.core SSH FAIL UNKNOWN — ПОДОЗРИТЕЛЬНАЯ КОПИЯ (см. access.log.bak)
T-10:42:08  internal.core SCP OK  /mnt/archive/keyparts — выгрузка частей ключа

ВЫВОД: существуют минимум две версии минуты 10:42:01. Вторая версия удобна для снятия ответственности с «неизвестного субъекта».

Статус расследования: ERASED (процедурно) / CONTROLLED (фактически)
```

---

### 28.6. `subject_roster.csv`

```csv
subject_id,role,sample_status,last_seen
SUBJ-001,operator_7,ACTIVE,2027-03-14T03:12:00Z
SUBJ-002,guest,REJECTED,2027-03-13T22:01:00Z
SUBJ-003,ghost00,UNSTABLE,2027-03-12T18:44:00Z
SUBJ-004,{PLAYER_HANDLE},PENDING,2027-03-14T03:15:00Z
```

**Примечание:** движок подставляет псевдоним игрока или строку `REDACTED`, если имени нет.

---

### 28.7. `signing_order.md`

```markdown
# Порядок подписей (восстановлено из черновиков)

1. **PRISM (Legal Proxy)** — одобрение «human-as-sample» в формулировках для PR.
2. **CIPHER (Classified review)** — перенос ответственности за «инциденты» в закрытую плоскость.
3. **MIRROR (system)** — фиксация паттерна решений; подпись не человеческая.

Вопрос: кто уполномочен подписать **ERASED**, если подпись не человеческая, а последствия — человеческие?
```

---

### 28.8. `witness_requirements.txt`

```
ТРЕБОВАНИЯ К СВИДЕТЕЛЬСКОМУ ЭКСПОРТУ

1. Расшифрованный manifest.json с валидными sha256 вложений.
2. audit_hash из manifest.json должен совпасть с токеном на internal.core:
   export_token.witness содержит префикс WITNESS-TOKEN; привязка к audit_hash в witness-pack.
3. Сессия инициирована с физического терминала T-CLASS (VOIDTERM-SN-7744-K9F2 или эквивалент).

Без пункта 3 экспорт считается теоретическим — система может отказать в квитанции.
```

---

### 28.9. Обновление дерева ФС (дополнение к §9)

После расшифровки (динамически):

```
/mnt/archive/bundle_decrypted/
├── manifest.json
├── 00_README.txt
├── truth_memo.md
├── incident_chain.txt
├── subject_roster.csv
├── signing_order.md
└── witness_requirements.txt
```

### 28.10. Связь с `witness-pack` (псевдокод)

```
function witness-pack:
  manifest = read("/mnt/archive/bundle_decrypted/manifest.json")
  assert manifest.audit_hash matches export_token.witness binding rule
  for f in manifest.files:
    assert sha256(read(f.path)) == f.sha256
  write("/out/WITNESS_bundle.manifest", canonical_copy(manifest))
  print epilogue A
```

### 28.11. Ложный манифест (режим γ, опционально)

В honeypot после «расшифровки» может появляться `manifest.json` с:

- `"audit_hash": "deadbeef…"` (совпадает с ложным хэшем §22),
- или отсутствующим `subject_roster.csv`.

Игрок сравнивает с `witness_requirements.txt` на настоящем узле после выхода из γ.

---

*Конец документа v1.2.*
